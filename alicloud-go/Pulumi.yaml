name: ${PROJECT}
description: ${DESCRIPTION}
runtime: go
template:
  description: A minimal asfslfj AliCloud Go Pulumi program
  config:
    alicloud:region:
      description: The AliCloud region to deploy into
      default: cn-beijing
